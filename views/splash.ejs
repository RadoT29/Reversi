<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reversi</title>
</head>
<body>
    <main>
        <section id="left">
            <h1>Reversi</h1>
            <h2>The board game for two</h2>
            <img src="reversi.logo.PNG" alt="Logo of the game">

            <form action="/play" method="get"> -->
                <button type="submit">Play</button>
            </form> 
        </section>
        <section id="right">
            <h2>Won games:</h2>
            <h2>Completed games:</h2>
            <h2>Opponents online:</h2>
          <h3><%= startedGames %> 
                <%if (startedGames != 1) { %>
                    games
                <% } else { %>
                    game
                <% } %>
                started, 
                <%= finishedGames %> 
                <%if (finishedGames != 1) { %>
                    games
                <% } else { %>
                    game
                <% } %>
                completed,
                <%= playerCount %>
                <%if (playerCount != 1) { %>
                    players
                <% } else { %>
                    player
                <% } %>
                online
            </h3>
        </section>
    </main>
    
</body>
</html>